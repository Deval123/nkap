spring:
  application:
    name: nkap-simulator-mpesa
  jackson:
    # Durations in scenario documents are ISO-8601 strings ("PT2S"), as the deployable's own
    # application.yml sets them. See ADR 0002 and the control plane contract.
    serialization:
      write-durations-as-timestamps: false
